# 识别悬浮框重新设计方案

## 一、当前设计分析

### 1.1 现有布局
```
┌────────────────────────────────────────────────────────────────────┐
│ [动画球+状态] │ [分隔线] │ [应用图标+名称] │ [识别文本区域]        │
│    70px      │   1px   │      90px       │       剩余空间         │
└────────────────────────────────────────────────────────────────────┘
                              720 x 90 px
```

### 1.2 现有问题

| 问题 | 描述 |
|------|------|
| **布局拥挤** | 三栏布局让信息层次不清晰，元素之间过于紧凑 |
| **视觉重心分散** | 动画球、应用信息、识别文本三者争夺注意力 |
| **配色单调** | 深色背景 `rgba(25,25,30,240)` 过于沉闷，缺乏活力 |
| **状态文字过小** | 9pt 字体在屏幕底部难以阅读 |
| **应用信息位置尴尬** | 夹在动画和文本之间，割裂了视觉流 |
| **动画球尺寸有限** | 60x60 的尺寸在 720px 宽的窗口中显得小气 |
| **缺乏品牌感** | 没有体现产品特色，与其他语音工具无差异 |

---

## 二、设计目标

1. **清晰的信息层次** - 用户一眼能看到最重要的信息
2. **流畅的状态过渡** - 状态变化自然，不突兀
3. **现代美观** - 符合 2024 年的设计趋势
4. **轻量不干扰** - 悬浮但不遮挡，简洁但不简陋
5. **品牌识别** - 有独特的视觉语言

---

## 三、左侧融合布局方案

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  ┌──────────┐   Chrome · 正在聆听                           │
│  │ 📱       │                                               │
│  │   🔵     │   识别结果文本显示在这里，支持多行显示...     │
│  │          │                                               │
│  └──────────┘                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
     100px                      460px
                    560 x 80 px
```

### 3.2 左侧面板详细设计

左侧面板将应用图标与动画球融合为一个整体视觉单元：

```
┌─────────────────────┐
│                     │
│    ┌───────────┐    │
│    │  📱 28px  │    │   <- 应用图标：圆角方形，带微光边框
│    │           │    │
│    └───────────┘    │
│                     │
│    ┌───────────┐    │
│    │           │    │
│    │    🔵     │    │   <- 动画球：48px，波形动画
│    │   48px    │    │
│    │           │    │
│    └───────────┘    │
│                     │
└─────────────────────┘
        100px

垂直排列：
- 顶部边距: 8px
- 应用图标: 28x28px
- 间距: 4px
- 动画球: 48x48px
- 底部边距: 自适应
```

#### 应用图标样式
```css
/* 应用图标容器 */
.app-icon {
    width: 28px;
    height: 28px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

/* 无图标时的默认样式 */
.app-icon-default {
    background: linear-gradient(135deg,
        rgba(100, 100, 100, 0.3),
        rgba(60, 60, 60, 0.3)
    );
}
```

### 3.3 右侧内容区详细设计

```
┌────────────────────────────────────────────────────┐
│                                                    │
│  Chrome · 正在聆听                          12pt  │  <- 状态行
│                                                    │
│  识别结果文本显示在这里，支持多行           14pt  │  <- 文本区
│  第二行文本继续显示...                            │
│                                                    │
└────────────────────────────────────────────────────┘
                     460px
```

#### 状态行设计
```
格式: [应用名称] · [状态文字]

示例:
- "Chrome · 正在聆听"
- "VS Code · 识别中"
- "微信 · 完成"

规则:
- 应用名称: 最多8个字符，超出截断+"…"
- 中间点: " · " (带空格的居中点)
- 状态文字: 根据当前状态变化，带对应颜色
```

#### 文本区设计
```
- 字体: 14pt, 行高 1.4
- 颜色: rgba(255, 255, 255, 0.9)
- 最大行数: 2行
- 超出处理: 末尾淡出效果 (渐变遮罩)
- 垂直对齐: 顶部对齐
```

### 3.4 完整尺寸规格

| 元素 | 当前值 | 新值 | 说明 |
|------|--------|------|------|
| **窗口宽度** | 720px | 560px | 更紧凑 |
| **窗口高度** | 90px | 80px | 略微降低 |
| **圆角** | 10px | 40px | 胶囊形状 |
| **左侧面板宽度** | 70px | 100px | 容纳图标+动画 |
| **应用图标** | 32px | 28px | 稍小，更精致 |
| **动画球** | 60px | 48px | 略小但更聚焦 |
| **内边距** | 10px | 16px | 更宽松 |
| **距屏幕底部** | 40px | 60px | 更舒适 |

---

## 四、配色方案

### 4.1 状态色板

| 状态 | 主色 | 辅色 | 状态文字色 | 背景渐变起点 |
|------|------|------|------------|--------------|
| **聆听中** | `#00D9FF` | `#00FFB3` | `#00D9FF` | `rgba(0, 180, 220, 0.08)` |
| **识别中** | `#FFB84D` | `#FF8A4D` | `#FFB84D` | `rgba(255, 150, 50, 0.08)` |
| **完成** | `#00E676` | `#69F0AE` | `#00E676` | `rgba(0, 200, 100, 0.08)` |
| **错误** | `#FF5252` | `#FF8A80` | `#FF5252` | `rgba(255, 80, 80, 0.08)` |

### 4.2 背景渐变

```css
/* 基础背景 */
.container {
    background: linear-gradient(
        135deg,
        rgba(var(--state-color), 0.08) 0%,
        rgba(20, 20, 25, 0.95) 100%
    );
}

/* 聆听中 */
.state-listening {
    background: linear-gradient(
        135deg,
        rgba(0, 180, 220, 0.10) 0%,
        rgba(15, 25, 35, 0.95) 100%
    );
}

/* 识别中 */
.state-recognizing {
    background: linear-gradient(
        135deg,
        rgba(255, 150, 50, 0.10) 0%,
        rgba(35, 25, 15, 0.95) 100%
    );
}

/* 完成 */
.state-done {
    background: linear-gradient(
        135deg,
        rgba(0, 200, 100, 0.10) 0%,
        rgba(15, 30, 20, 0.95) 100%
    );
}
```

### 4.3 边框与阴影

```css
.container {
    /* 边框 - 更明显的亮边 */
    border: 1px solid rgba(255, 255, 255, 0.12);

    /* 多层阴影 */
    box-shadow:
        0 4px 24px rgba(0, 0, 0, 0.25),       /* 主阴影 */
        0 1px 3px rgba(0, 0, 0, 0.15),        /* 近阴影 */
        inset 0 1px 0 rgba(255, 255, 255, 0.08); /* 顶部高光 */
}
```

---

## 五、各状态视觉效果

### 5.1 聆听中 (Listening)

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────┐                                               │
│  │ 📱       │   Chrome · 正在聆听        <- 青蓝色 #00D9FF  │
│  │   🔵     │                                               │
│  │ (波动)   │   [等待语音输入...]        <- 浅灰占位符      │
│  └──────────┘                                               │
└─────────────────────────────────────────────────────────────┘
  背景: 青蓝色渐变微光
  动画球: 青蓝色，平缓波动，向外辐射涟漪
```

### 5.2 识别中 (Recognizing)

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────┐                                               │
│  │ 📱       │   Chrome · 识别中          <- 橙色 #FFB84D    │
│  │   🟠     │                                               │
│  │ (脉动)   │   今天天气怎么样           <- 黄色文字        │
│  └──────────┘                                               │
└─────────────────────────────────────────────────────────────┘
  背景: 暖橙色渐变微光
  动画球: 橙色，快速脉动，收缩效果
  文本: #FFE066 黄色，表示中间结果
```

### 5.3 完成 (Done)

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────┐                                               │
│  │ 📱       │   Chrome · 完成            <- 绿色 #00E676    │
│  │   🟢     │                                               │
│  │ (静止)   │   今天天气怎么样？         <- 白色最终文本    │
│  └──────────┘                                               │
└─────────────────────────────────────────────────────────────┘
  背景: 绿色渐变微光
  动画球: 绿色，停止波动，轻微放大后回弹
  文本: rgba(255,255,255,0.95) 白色
  行为: 500ms 后自动隐藏
```

### 5.4 错误 (Error)

```
┌─────────────────────────────────────────────────────────────┐
│  ┌──────────┐                                               │
│  │ 📱       │   Chrome · 错误            <- 红色 #FF5252    │
│  │   🔴     │                                               │
│  │ (静止)   │   网络连接失败             <- 红色错误信息    │
│  └──────────┘                                               │
└─────────────────────────────────────────────────────────────┘
  背景: 红色渐变微光
  动画球: 红色，停止动画
  文本: rgba(255,255,255,0.7) 略暗
  行为: 1500ms 后自动隐藏
```

---

## 六、交互动画

### 6.1 窗口出现动画

```python
# 动画参数
duration = 200ms
easing = ease-out-cubic

# 动画效果
1. 位置: 从下方 20px 处滑入
2. 透明度: 0 -> 1
3. 缩放: 0.95 -> 1.0 (可选，增加弹性感)
```

### 6.2 窗口消失动画

```python
# 动画参数
duration = 150ms
easing = ease-in

# 动画效果
1. 位置: 向下滑出 15px
2. 透明度: 1 -> 0
```

### 6.3 状态切换过渡

```python
# 背景颜色过渡
transition-duration = 300ms
transition-timing = ease-out

# 动画球颜色过渡
color_lerp_factor = 0.15  # 每帧插值 15%

# 状态文字
- 颜色渐变过渡
- 无位置变化（保持稳定）
```

### 6.4 文本更新动画

```python
# 部分识别结果更新时
- 无动画，直接替换（避免闪烁）
- 自动滚动到最新内容

# 最终结果显示时
- 颜色从黄色渐变到白色
- 时长: 200ms
```

---

## 七、实现优先级

### P0 - 必须实现

| 序号 | 改动项 | 描述 |
|------|--------|------|
| 1 | 窗口尺寸 | 720x90 → 560x80 |
| 2 | 胶囊圆角 | 10px → 40px |
| 3 | 左侧面板重构 | 应用图标 + 动画球垂直排列 |
| 4 | 移除分隔线 | 简化布局 |
| 5 | 状态行格式 | "[应用名] · [状态]" |
| 6 | 字体大小 | 状态 12pt，文本 14pt |
| 7 | 背景渐变 | 添加状态色微光 |

### P1 - 推荐实现

| 序号 | 改动项 | 描述 |
|------|--------|------|
| 1 | 窗口动画 | 滑入/滑出效果 |
| 2 | 背景过渡 | 状态切换时渐变 |
| 3 | 边框优化 | 更明显的毛玻璃边框 |
| 4 | 文本限制 | 最多2行 + 末尾淡出 |

### P2 - 可选实现

| 序号 | 改动项 | 描述 |
|------|--------|------|
| 1 | 动画球升级 | 呼吸光环效果 |
| 2 | 动态宽度 | 根据内容自适应 |

---

## 八、视觉对比

### Before（当前）
```
┌─────────────────────────────────────────────────────────────────────┐
│ 🔵 listening │ │ 📱 Chrome │ 识别出的文本内容...                     │
└─────────────────────────────────────────────────────────────────────┘
  60px 动画      1px  90px
       9pt状态          8pt名称              11pt 文本
                 720px, 圆角10px

问题：
- 三栏布局割裂，视觉流被打断
- 动画球太小（60px）
- 状态文字 9pt 太小难读
- 背景纯色沉闷
- 分隔线增加视觉噪声
```

### After（改进后）
```
      ┌─────────────────────────────────────────────────────────┐
      │                                                         │
      │  ┌────┐   Chrome · 正在聆听                    12pt    │
      │  │📱  │                                                 │
      │  │🔵  │   识别出的文本内容显示在这里...        14pt    │
      │  └────┘                                                 │
      │                                                         │
      └─────────────────────────────────────────────────────────┘
        100px                 460px
                   560px, 圆角40px (胶囊)

改进：
- 左侧融合：应用图标 + 动画球成为整体
- 两栏布局：左侧信息区 + 右侧内容区
- 状态行：应用名与状态合并，更清晰
- 字体更大更清晰
- 胶囊形状更现代
- 渐变背景增加层次感
```

---

## 九、技术实现要点

### 9.1 左侧面板布局代码

```python
# 左侧面板：应用图标 + 动画球
left_panel = QWidget()
left_panel.setFixedWidth(100)
left_layout = QVBoxLayout(left_panel)
left_layout.setContentsMargins(16, 8, 8, 8)
left_layout.setSpacing(4)
left_layout.setAlignment(Qt.AlignmentFlag.AlignCenter)

# 应用图标
self._app_icon_label = QLabel()
self._app_icon_label.setFixedSize(28, 28)
self._app_icon_label.setStyleSheet("""
    background: rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.12);
""")
left_layout.addWidget(self._app_icon_label, 0, Qt.AlignmentFlag.AlignCenter)

# 动画球
self._wave_widget = WaveOrbWidget()
self._wave_widget.setFixedSize(48, 48)
left_layout.addWidget(self._wave_widget, 0, Qt.AlignmentFlag.AlignCenter)
```

### 9.2 状态行代码

```python
# 状态行：应用名 + 状态
self._status_line = QLabel()
self._status_line.setStyleSheet("""
    color: rgba(255, 255, 255, 0.7);
    font-size: 12pt;
    font-weight: 500;
""")

def update_status(self, app_name: str, status: str, status_color: str):
    # 截断应用名
    if len(app_name) > 8:
        app_name = app_name[:7] + "…"

    # 设置富文本
    self._status_line.setText(
        f'{app_name} · <span style="color: {status_color}">{status}</span>'
    )
    self._status_line.setTextFormat(Qt.TextFormat.RichText)
```

### 9.3 背景渐变代码

```python
def _get_gradient_style(self, mode: str) -> str:
    gradients = {
        "recording": "rgba(0, 180, 220, 0.10), rgba(15, 25, 35, 0.95)",
        "recognizing": "rgba(255, 150, 50, 0.10), rgba(35, 25, 15, 0.95)",
        "done": "rgba(0, 200, 100, 0.10), rgba(15, 30, 20, 0.95)",
        "error": "rgba(255, 80, 80, 0.10), rgba(35, 15, 15, 0.95)",
    }
    colors = gradients.get(mode, gradients["recording"])

    return f"""
        QWidget#container {{
            background: qlineargradient(
                x1:0, y1:0, x2:1, y2:1,
                stop:0 {colors.split(',')[0]},
                stop:1 {colors.split(',')[1]}
            );
            border-radius: 40px;
            border: 1px solid rgba(255, 255, 255, 0.12);
        }}
    """
```

### 9.4 窗口动画代码

```python
from PySide6.QtCore import QPropertyAnimation, QParallelAnimationGroup, QPoint

def show_with_animation(self):
    self._center_on_screen()
    target_pos = self.pos()

    # 初始位置：下方 20px
    self.move(target_pos.x(), target_pos.y() + 20)
    self.setWindowOpacity(0)
    self.show()

    # 位置动画
    self._pos_anim = QPropertyAnimation(self, b"pos")
    self._pos_anim.setDuration(200)
    self._pos_anim.setEndValue(target_pos)
    self._pos_anim.setEasingCurve(QEasingCurve.Type.OutCubic)

    # 透明度动画
    self._opacity_anim = QPropertyAnimation(self, b"windowOpacity")
    self._opacity_anim.setDuration(200)
    self._opacity_anim.setEndValue(1.0)

    # 并行执行
    self._show_anim_group = QParallelAnimationGroup()
    self._show_anim_group.addAnimation(self._pos_anim)
    self._show_anim_group.addAnimation(self._opacity_anim)
    self._show_anim_group.start()

def hide_with_animation(self):
    current_pos = self.pos()

    # 位置动画：向下滑出
    self._pos_anim = QPropertyAnimation(self, b"pos")
    self._pos_anim.setDuration(150)
    self._pos_anim.setEndValue(QPoint(current_pos.x(), current_pos.y() + 15))
    self._pos_anim.setEasingCurve(QEasingCurve.Type.InCubic)

    # 透明度动画
    self._opacity_anim = QPropertyAnimation(self, b"windowOpacity")
    self._opacity_anim.setDuration(150)
    self._opacity_anim.setEndValue(0.0)

    # 动画结束后隐藏
    self._hide_anim_group = QParallelAnimationGroup()
    self._hide_anim_group.addAnimation(self._pos_anim)
    self._hide_anim_group.addAnimation(self._opacity_anim)
    self._hide_anim_group.finished.connect(self.hide)
    self._hide_anim_group.start()
```

---

## 十、下一步

1. 确认设计细节无误后开始实现
2. 按 P0 → P1 → P2 顺序迭代
3. 每个阶段完成后测试并收集反馈
